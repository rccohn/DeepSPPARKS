# from pathlib import Path


def main():
    #  datasaet root, contains folders for train, val, test
    #  target, where to save data
    # extract both node and edge patches
    #
    # data_root
    # - train, val, test
    #   - 1.json, 2.json, ...

    # patches saved as new dataset (candidate_grain_small_{nodes, edges}_{r}_{c}_v2.0.0)
    # where r, c indicate patch size
    # move patch extraction to deepspparks itself?
    #
    pass


if __name__ == "__main__":
    main()
